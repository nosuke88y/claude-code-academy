# Claude Code Tips & テクニック 完全ガイド

作成日: 2026-04-17
対象: Claude Code CLI / Desktop / Web版

凡例:
- 難易度: [初級] [中級] [上級]
- 効果度: [高] [中] [低]

---

## 1. UI・操作系

### キーボードショートカット

| # | Tips | 難易度 | 効果度 | 説明 |
|---|------|--------|--------|------|
| 1 | `Shift+Tab` でPermission modeを即切替 | 初級 | 高 | Ask → Auto Edit → Full Auto をワンキーで切替。作業フェーズに応じて瞬時に変更可能 |
| 2 | `Ctrl+R` でコマンド履歴を逆検索 | 初級 | 中 | 過去のプロンプトをインクリメンタル検索。同じ指示の再利用が高速に |
| 3 | `Ctrl+O` でトランスクリプトビューア表示 | 中級 | 高 | ツール呼び出しの詳細ログを確認。Claude Codeの内部動作が丸見えになる |
| 4 | `\` + `Enter` で複数行入力 | 初級 | 高 | 全ターミナル対応の改行方法。長いプロンプトを構造化して入力可能 |
| 5 | `Ctrl+V` で画像をクリップボードから貼付 | 中級 | 高 | スクリーンショットやUI画像を直接貼り付けてレビュー依頼。エラー画面の分析に最適 |
| 6 | `Ctrl+B` でバックグラウンドタスク実行 | 中級 | 中 | 長時間ビルドやテストをバックグラウンドで実行しながら別作業を続行 |
| 7 | `Ctrl+T` でタスクリスト表示/非表示 | 中級 | 中 | バックグラウンドタスクの進捗をリアルタイム確認 |
| 8 | `Alt+P` でモデルを即切替 | 中級 | 高 | Opus ↔ Sonnet ↔ Haiku をキー一つで切替。重いタスクはOpus、軽いタスクはHaikuに |
| 9 | `Alt+T` で Extended Thinking を切替 | 中級 | 中 | 複雑な推論が必要な時だけONにしてトークン節約 |
| 10 | `Alt+O` で高速モード（Fast Mode）切替 | 初級 | 中 | 同じOpusモデルで高速出力。品質は変わらず速度だけ向上 |
| 11 | `Ctrl+G` で外部エディタで編集 | 上級 | 低 | 長文プロンプトをVS Codeなどで編集してから送信 |
| 12 | `Ctrl+X Ctrl+K` で全バックグラウンドエージェント終了 | 上級 | 低 | 暴走したサブエージェントを一括停止（2回押しで確認） |

### Vim Mode

| # | Tips | 難易度 | 効果度 | 説明 |
|---|------|--------|--------|------|
| 13 | `/config` → Editor mode → "Vim" で有効化 | 中級 | 中 | hjkl移動、dd削除、yy/pコピペなどVimキーバインドが使える。Vimmerに必須 |
| 14 | Vim modeとキーバインドは独立動作 | 中級 | 低 | EscでNORMALモードに入るが、Ctrl系ショートカットはそのまま使える |

### 画面・IDE連携

| # | Tips | 難易度 | 効果度 | 説明 |
|---|------|--------|--------|------|
| 15 | VS Code拡張でエディタから直接Claude Code起動 | 初級 | 高 | Anthropic公式拡張。ファイルをコンテキストに自動含有 |
| 16 | JetBrains拡張（IntelliJ, WebStorm等）対応 | 初級 | 高 | IDE内でClaude Codeをシームレスに実行 |
| 17 | Desktop版のSide Chats機能 | 中級 | 中 | メインスレッドに影響なく別質問が可能。設計相談しながら実装を進められる |
| 18 | Desktop版のLive App Preview | 中級 | 高 | devサーバー・HTML・PDFをDesktopアプリ内でプレビュー表示 |
| 19 | Desktop版のVisual Diff Review | 中級 | 高 | インラインコメント付きの差分ビューア。PRレビューに最適 |

---

## 2. プロンプト・指示系

### スラッシュコマンド

| # | Tips | 難易度 | 効果度 | 説明 |
|---|------|--------|--------|------|
| 20 | `/compact [指示]` で焦点を絞って圧縮 | 初級 | 高 | `/compact パフォーマンス改善に集中` のように焦点を指定すると、関連情報を優先保持 |
| 21 | `/batch <指示>` で大規模変更を並列処理 | 上級 | 高 | 5〜30個の独立した変更を自動分割・並列実行。大規模リファクタリングの切り札 |
| 22 | `/review` でPRレビューを自動実行 | 初級 | 高 | 現在のdiffまたは指定PRを自動レビュー。セキュリティ・パフォーマンス・可読性を分析 |
| 23 | `/security-review` でセキュリティ監査 | 中級 | 高 | 保留中の変更をOWASP観点でセキュリティレビュー |
| 24 | `/init` でCLAUDE.mdを自動生成 | 初級 | 高 | プロジェクト構造を分析して最適なCLAUDE.mdを自動作成 |
| 25 | `/diff` でインタラクティブdiffビューア | 中級 | 中 | 変更内容を視覚的に確認。コミット前の最終チェックに |
| 26 | `/cost` でトークン使用量を確認 | 初級 | 中 | セッションのコスト・トークン消費をリアルタイム把握 |
| 27 | `/context` でコンテキスト使用状況を可視化 | 中級 | 中 | 何がコンテキストを占有しているか一目で確認 |
| 28 | `/effort [level]` で推論の深さを調整 | 中級 | 中 | low/medium/high/xhigh/max の5段階。簡単なタスクはlowで高速化 |
| 29 | `/branch [name]` で会話をブランチ | 上級 | 中 | 現在の時点から会話を分岐。別のアプローチを試して比較可能 |
| 30 | `/export [filename]` で会話をエクスポート | 初級 | 低 | テキスト形式で保存。ナレッジ共有やドキュメント化に |
| 31 | `/insights` でセッション使用分析 | 中級 | 低 | 過去のセッション傾向をレポート。使い方の改善ポイントが見える |
| 32 | `/team-onboarding` でチーム用ガイド生成 | 中級 | 中 | チームメンバー向けオンボーディングガイドを自動生成 |
| 33 | `/simplify` でコード品質を自動改善 | 中級 | 中 | 変更されたファイルをレビューし、品質を自動で改善 |

### @記法（コンテキスト参照）

| # | Tips | 難易度 | 効果度 | 説明 |
|---|------|--------|--------|------|
| 34 | `@ファイルパス` でファイルを明示的に参照 | 初級 | 高 | `@src/api/auth.ts` のように指定。タブ補完が効く |
| 35 | `@フォルダ/` でディレクトリ全体を参照 | 初級 | 高 | `@src/components/` で関連ファイルをまとめてコンテキストに投入 |
| 36 | `@URL` でWebページを参照 | 中級 | 中 | 外部ドキュメントやAPI仕様を直接参照して作業に反映 |
| 37 | `!コマンド` でBashを直接実行 | 初級 | 中 | プロンプトで `!` をつけるとClaude Codeを経由せず直接コマンド実行 |

### 効果的な指示パターン

| # | Tips | 難易度 | 効果度 | 説明 |
|---|------|--------|--------|------|
| 38 | 「〜して。テストも書いて」の一文追加 | 初級 | 高 | 修正指示の末尾に「テストも書いて」を加えるだけでTDD的なワークフローに |
| 39 | 「最小限の変更で」と明示する | 初級 | 高 | スコープクリープ（範囲拡大）を防ぐ。特にバグ修正やhotfixで重要 |
| 40 | 番号付きステップで段階的に指示 | 初級 | 高 | 「1. 分析 2. 修正 3. テスト 4. レビュー」のように段階を明示 |
| 41 | 「〜の観点で」とレビュー観点を限定 | 中級 | 高 | 「パフォーマンスの観点でレビュー」のように焦点を絞ると精度向上 |
| 42 | ネガティブ制約を明示する | 中級 | 中 | 「既存のテストを壊さないで」「新しい依存を追加しないで」等 |

### CLAUDE.md 活用テクニック

| # | Tips | 難易度 | 効果度 | 説明 |
|---|------|--------|--------|------|
| 43 | CLAUDE.mdに`@ファイル名`でインポート | 中級 | 高 | `@README.md` `@package.json` 等をCLAUDE.md内で参照。常に最新情報を自動取得 |
| 44 | サブディレクトリにCLAUDE.mdを配置 | 中級 | 中 | `src/api/CLAUDE.md` のようにモジュール別の指示を配置。そのディレクトリのファイルを読む時に自動適用 |
| 45 | CLAUDE.local.mdで個人設定を分離 | 中級 | 中 | `.gitignore`に入れる個人設定用。チーム共有のCLAUDE.mdと個人の好みを分離 |
| 46 | 4段階の優先順位を理解する | 上級 | 中 | 管理者 > プロジェクト > ユーザー > ローカル の順で適用。組織のポリシーを上書きできない設計 |
| 47 | 開発コマンド一覧をCLAUDE.mdに記載 | 初級 | 高 | `npm run dev`, `npm run test` 等を書いておくとClaude Codeが正しいコマンドを使う |

---

## 3. ワークフロー系

### git worktreeとの組み合わせ

| # | Tips | 難易度 | 効果度 | 説明 |
|---|------|--------|--------|------|
| 48 | `claude -w feature-name` でworktree隔離セッション | 上級 | 高 | 自動でworktreeを作成し、メインブランチに影響なく作業。複数機能の並列開発に |
| 49 | `--tmux` でtmuxペイン付きworktree | 上級 | 中 | `claude -w feature --tmux` でエディタ・ターミナル・プレビューを分割表示 |
| 50 | `.worktreeinclude` でgitignoreファイルをコピー | 上級 | 高 | `.env`, `.env.local` 等をworktreeに自動コピー。環境変数が引き継がれる |
| 51 | `symlinkDirectories` でnode_modulesを共有 | 上級 | 中 | settings.jsonで`node_modules`をシンボリックリンク。ディスク節約＆npm install不要 |

### セッション管理

| # | Tips | 難易度 | 効果度 | 説明 |
|---|------|--------|--------|------|
| 52 | `claude -c` で最新セッションを即再開 | 初級 | 高 | 前回の会話をそのまま続行。朝イチの作業再開に最適 |
| 53 | `claude --resume "名前"` で名前付きセッション復帰 | 中級 | 高 | `/rename auth-refactor` でセッションに名前をつけておくと後から探しやすい |
| 54 | `claude -n "セッション名"` で名前付き新規セッション | 中級 | 中 | 最初から名前をつけて開始。プロジェクト・機能別にセッションを整理 |
| 55 | `/color [色]` でセッションのプロンプトバー色変更 | 初級 | 低 | 複数ターミナルで作業する際の視覚的区別に |

### タスクの分割・委譲パターン

| # | Tips | 難易度 | 効果度 | 説明 |
|---|------|--------|--------|------|
| 56 | 「並列で実行して」と明示する | 中級 | 高 | サブエージェントによる並列処理を明示的にトリガー。調査タスクで特に効果的 |
| 57 | 「調査→実装」の2フェーズで進める | 中級 | 高 | Phase1で並列調査、Phase2で結果に基づき順次実装。品質と速度を両立 |
| 58 | ディレクトリ単位でタスクを分割 | 中級 | 高 | 「src/api/は〜、src/components/は〜、tests/は〜」のように分割すると並列処理しやすい |
| 59 | `/loop [間隔] [プロンプト]` で定期実行 | 上級 | 中 | `/loop 5m check if build passes` のように監視タスクを自動化 |

### Web版専用機能

| # | Tips | 難易度 | 効果度 | 説明 |
|---|------|--------|--------|------|
| 60 | `claude --remote "タスク"` でクラウドセッション | 上級 | 高 | Anthropicのクラウド VM上で実行。ローカルマシンに負荷をかけずに長時間タスクを処理 |
| 61 | `/teleport` でクラウド→ローカルにハンドオフ | 上級 | 中 | Web版で始めた作業をローカルCLIに引き継ぎ |
| 62 | `/autofix-pr` でPR自動修正 | 上級 | 高 | PRを監視し、CI失敗やレビューコメントに自動対応 |
| 63 | `/ultraplan` でクラウド計画立案 | 上級 | 中 | 複雑な計画をクラウドで草案作成・検証 |
| 64 | `/ultrareview` で多エージェントPRレビュー | 上級 | 高 | クラウドで複数の観点から深いPRレビューを実行 |

---

## 4. 設定・カスタマイズ系

### Permission modes の使い分け

| # | Tips | 難易度 | 効果度 | 説明 |
|---|------|--------|--------|------|
| 65 | `plan` モードで計画だけ立てさせる | 中級 | 高 | `claude --permission-mode plan` でファイル変更禁止。設計レビューや調査に最適 |
| 66 | `allow/deny` でツール単位の細かい制御 | 中級 | 高 | `"allow": ["Bash(npm run *)"]` のようにパターンマッチで許可。`Bash(git push *)`だけ許可等 |
| 67 | `deny` で危険なコマンドをブロック | 中級 | 高 | `"deny": ["Bash(curl *)", "Read(./.env)"]` でデータ漏洩リスクを排除 |
| 68 | `auto` モードでバックグラウンド安全チェック | 上級 | 中 | 自動承認しつつ、裏でセキュリティチェックが走る。信頼できるプロジェクト向け |

### Custom slash commands / Skills

| # | Tips | 難易度 | 効果度 | 説明 |
|---|------|--------|--------|------|
| 69 | `.claude/skills/` でスキルを作成（推奨） | 中級 | 高 | SKILL.mdのfrontmatterでツール制限・モデル指定が可能。.claude/commands/より高機能 |
| 70 | `$ARGUMENTS` で引数を受け取る | 中級 | 高 | `/project:fix-issue 123` → `$ARGUMENTS`に"123"が入る。汎用コマンドの作成に |
| 71 | `$0`, `$1` で複数引数を分解 | 上級 | 中 | `/project:migrate Button React Vue` → `$0`=Button, `$1`=React, `$2`=Vue |
| 72 | `allowed-tools` でスキル内のツール制限 | 上級 | 中 | SKILL.mdのfrontmatterで`allowed-tools: Read Grep`のように安全に制限 |
| 73 | ユーザースキル vs プロジェクトスキル | 中級 | 中 | `~/.claude/skills/`は全プロジェクト共通、`.claude/skills/`はチーム共有（git管理） |
| 74 | `${CLAUDE_SESSION_ID}` でログ管理 | 上級 | 低 | スキル内でセッションIDを参照。デバッグログの紐付けに |

### Hooks の活用パターン

| # | Tips | 難易度 | 効果度 | 説明 |
|---|------|--------|--------|------|
| 75 | `PreToolUse` で危険操作をブロック | 中級 | 高 | 本番DB接続文字列を含むコマンドを実行前にブロック |
| 76 | `PostToolUse` で自動lint実行 | 中級 | 高 | `matcher: "Edit\|Write"` でファイル変更後にESLint自動修正 |
| 77 | `UserPromptSubmit` でプロンプト検証 | 上級 | 中 | ユーザー入力時にコンテキスト注入や入力バリデーションを実行 |
| 78 | `SessionStart` で環境初期化 | 上級 | 中 | セッション開始時に環境変数チェックやDBマイグレーション確認を自動実行 |
| 79 | `Notification` でデスクトップ通知 | 中級 | 中 | タスク完了時にOS通知を送信。長時間タスクの完了を見逃さない |
| 80 | `PermissionRequest` で権限決定をオーバーライド | 上級 | 中 | プログラマブルに権限判定。CI環境での自動承認等に |
| 81 | matcherで正規表現パターン | 上級 | 中 | `"matcher": "mcp__memory__.*"` でMCPツール全体にマッチ |
| 82 | 終了コード2でブロッキングエラー | 上級 | 高 | Hook内で`exit 2`を返すとツール実行を完全ブロック。安全ガードの実装に |

---

## 5. 外部ツール連携系

### MCPサーバー

| # | Tips | 難易度 | 効果度 | 説明 |
|---|------|--------|--------|------|
| 83 | `/mcp` で接続中のMCPサーバーを管理 | 中級 | 中 | 接続状態の確認、サーバーの追加・削除を対話的に実行 |
| 84 | `.mcp.json` でプロジェクト固有のMCP設定 | 中級 | 高 | プロジェクトルートに配置。チームで共有できるMCP設定 |
| 85 | `--mcp-config` でMCP設定ファイルを指定 | 上級 | 中 | `claude --mcp-config ./mcp.json` で起動時に設定を指定 |
| 86 | 公式MCPサーバー（filesystem, GitHub, PostgreSQL, Puppeteer） | 中級 | 高 | Anthropic提供で品質保証済み。これだけで主要ユースケースをカバー |
| 87 | 複数MCPの組み合わせワークフロー | 上級 | 高 | GitHub MCP + DB MCP + Slack MCP でIssue→修正→PR→通知を完全自動化 |
| 88 | 管理者MCP設定で組織的に制御 | 上級 | 中 | `managed-mcp.json` で許可/禁止サーバーを組織レベルで管理 |

### CI/CDへの組み込み

| # | Tips | 難易度 | 効果度 | 説明 |
|---|------|--------|--------|------|
| 89 | `claude -p` で非対話モード実行 | 中級 | 高 | GitHub ActionsなどCIパイプラインから直接呼び出し。`-p "query"` で結果を取得 |
| 90 | `--output-format json` で構造化出力 | 中級 | 高 | CI/CDスクリプトで結果をパースしやすい形式で出力 |
| 91 | `--max-turns N` でターン数を制限 | 中級 | 高 | CIでの無限ループ防止。`--max-turns 5` で安全に制限 |
| 92 | `--max-budget-usd N` でコスト上限設定 | 中級 | 高 | `--max-budget-usd 5.00` で予算超過を防止。CI環境で必須 |
| 93 | `--no-session-persistence` でセッション保存なし | 中級 | 中 | CI環境でのセッションデータ蓄積を防止 |
| 94 | `cat file | claude -p "query"` でパイプ入力 | 中級 | 高 | CIで生成されたファイルやログをそのまま分析に投入 |
| 95 | `--json-schema` で出力スキーマを強制 | 上級 | 高 | CIで決まった形式のJSONを確実に取得 |

### 環境変数・APIキー管理

| # | Tips | 難易度 | 効果度 | 説明 |
|---|------|--------|--------|------|
| 96 | `CLAUDE_CODE_USE_BEDROCK=1` でAWS Bedrock経由 | 上級 | 中 | 企業のAWSアカウント経由でAPI利用。データ管理ポリシー準拠に |
| 97 | `CLAUDE_CODE_USE_VERTEX=1` でGoogle Vertex AI経由 | 上級 | 中 | GCP経由でAPI利用。Google Cloud環境との統合に |
| 98 | settings.jsonの`env`キーで環境変数設定 | 中級 | 中 | プロジェクト別にAPI Key等を管理。`"env": {"KEY": "value"}` |

---

## 6. トラブルシューティング系

### よくあるエラーと対処法

| # | Tips | 難易度 | 効果度 | 説明 |
|---|------|--------|--------|------|
| 99 | `/doctor` で環境診断 | 初級 | 高 | インストール・設定の問題を自動診断・修正。困ったらまずこれ |
| 100 | `/terminal-setup` でShift+Enterが効かない問題を解決 | 初級 | 高 | VS Code Terminal, Alacritty, Warp等でマルチライン入力を有効化 |
| 101 | `--debug "api,hooks"` でデバッグモード | 上級 | 高 | API通信やHook実行の詳細ログを出力。原因不明のエラー調査に |
| 102 | `--debug-file /tmp/debug.log` でデバッグログをファイルに | 上級 | 中 | 画面を汚さずにログを保存。後から詳細分析が可能 |

### コンテキスト管理

| # | Tips | 難易度 | 効果度 | 説明 |
|---|------|--------|--------|------|
| 103 | コンテキスト80%超えたら `/compact` | 初級 | 高 | 自動compactもあるが、手動で焦点を指定した方が重要情報の保持率が高い |
| 104 | `/clear` で完全リセット（復帰可能） | 初級 | 高 | `/resume` で戻れるので安心してリセット可能。話題が大きく変わる時に |
| 105 | CLAUDE.mdは自動compactでも保持される | 中級 | 高 | compact後もプロジェクト設定は全文再注入される。重要情報はCLAUDE.mdに書くべき |
| 106 | サブディレクトリのCLAUDE.mdはcompact後に再読み込み | 上級 | 中 | compact後は失われるが、そのディレクトリのファイルを次に読む時に自動再適用 |

### 意図しない動作を防ぐコツ

| # | Tips | 難易度 | 効果度 | 説明 |
|---|------|--------|--------|------|
| 107 | 「変更前にgit statusを確認して」と指示 | 初級 | 高 | 意図しない変更の検出。作業前後のdiffを確認させる習慣をつける |
| 108 | `--permission-mode plan` で確認してから実行 | 中級 | 高 | まず計画を見せてもらい、承認してから実行モードに切替 |
| 109 | `.claude/settings.json` をgitにコミット | 中級 | 高 | チーム全員が同じPermission設定で作業。設定ミスを防止 |
| 110 | `--system-prompt` で行動制約を追加 | 上級 | 中 | `--system-prompt "Never modify test files"` のように制約を追加 |
| 111 | `--append-system-prompt-file` でスタイルルール適用 | 上級 | 中 | チームのコーディングスタイルをファイルで管理して適用 |

---

## 7. 隠れた便利機能（これは知らなかった系）

| # | Tips | 難易度 | 効果度 | 説明 |
|---|------|--------|--------|------|
| 112 | `--bare` で最小限モード起動 | 上級 | 中 | スキル・MCP・CLAUDE.mdを全スキップ。高速起動でシンプルなクエリに |
| 113 | `--add-dir ../libs ../apps` で追加ディレクトリ | 中級 | 高 | monorepoで別パッケージのコードをコンテキストに含める |
| 114 | `/copy [N]` で直前の応答をクリップボードコピー | 初級 | 中 | Nを指定すると過去N番目の応答をコピー |
| 115 | `--fork-session` でセッションを分岐 | 上級 | 中 | 現在のコンテキストを保持したまま新セッションIDで開始。A/Bテストに |
| 116 | `CLAUDE_CODE_DISABLE_GIT_INSTRUCTIONS=1` | 上級 | 低 | Gitの自動指示を無効化。特殊なGitワークフローで干渉する場合に |
| 117 | Dispatch from Phone（Desktop版） | 中級 | 中 | iOSやAndroidのClaudeアプリからDesktopのClaude Codeにタスクを送信 |
| 118 | `--remote-control "プロジェクト名"` | 上級 | 中 | ローカルCLIセッションをWeb版から監視・操作可能に |
| 119 | `/plugin` でプラグイン管理 | 上級 | 低 | サードパーティプラグインの追加・管理 |
| 120 | `sparsePaths` でworktreeの部分チェックアウト | 上級 | 中 | 巨大monorepoで必要なパスだけworktreeにコピー。高速化に |

---

## Tips活用のおすすめフロー

### 初級者（まず覚える10個）
1, 4, 20, 22, 24, 34, 47, 52, 99, 103

### 中級者（生産性が跳ねる10個）
8, 15, 21, 43, 56, 65, 66, 75, 89, 113

### 上級者（マスターレベル）
48, 60, 64, 69, 82, 87, 92, 95, 101, 120
